# Toldot backend environment
# Copy this file to .env and fill in your key. The backend reads it at startup.

# OpenRouter API key (required — the Dungeon Master model runs via OpenRouter).
OPENROUTER_API_KEY=

# Optional: use the mock DM (no network) for fast local testing.
# MOCK_LLM=true
