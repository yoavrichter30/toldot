id: T081-Q06
parent_task_id: T081
milestone: M07
title: Add schedule_event effect and atomic execution
executor_profile: qwen_implementer
depends_on:
- T081-Q05
risk: low
context_policy:
  max_input_tokens: 12000
  max_files_in_context: 8
  include_git_diff: true
  never_include_entire_repository: true
change_budget:
  allowed_write_files:
  - src/simulation/systems/event_effect_executor.gd
  - tests/simulation/test_event_effect_executor.gd
  max_changed_lines: 110
  max_new_dependencies: 0
  max_production_files: 1
read_first:
- AGENTS.md
- docs/architecture.md
- docs/simulation_rules.md
objective: Apply a small whitelist of event option effects atomically.
exact_instructions:
- 'Implement only these effect types in this task: schedule_event.'
- Prevalidate target IDs and fields.
- Add one success and one failure test for each new effect.
- Do not execute arbitrary scripts.
- Prevalidate the full effect list before applying any effect.
- Add an atomic no-partial-mutation test and immediate self-loop rejection.
required_api_or_shapes: []
tests_or_checks_to_add:
- Add or update only tests directly required by this packet.
- Do not weaken, delete, skip, or rewrite an existing test to make the task pass.
acceptance_criteria:
- All new effect tests pass.
- Every registered effect has a test.
- Unknown target prevents all mutation.
- Created cohort receives a unique deterministic ID.
- Scheduled event cannot schedule itself in an immediate loop.
validation:
  level: targeted
  commands:
  - godot --headless --path . --script res://tools/test_runner.gd
  must_capture_exit_code: true
  must_include_relevant_failure_lines: true
forbidden:
- Do not modify files outside allowed_write_files.
- Do not refactor unrelated code.
- Do not add packages, addons, autoloads, editor plugins, or generated binaries.
- Do not invent a new API when required_api_or_shapes specifies one.
- Do not leave TODO, pass, placeholder returns, or commented-out failed attempts unless explicitly requested.
- Do not execute scripts from content.
stop_and_report_if:
- A required read file is missing.
- The requested API conflicts with already accepted code.
- A validation failure is outside the allowed write files.
- Completion would exceed max_changed_lines by more than 20 percent.
- Two focused repair attempts fail.
required_response:
  format: yaml
  fields:
  - task_id
  - status
  - summary
  - files_changed
  - commands_run
  - command_results
  - acceptance_checklist
  - remaining_problem
  status_values:
  - completed
  - blocked
  - failed
  no_markdown_fences: true
status: pending
resolved_owner: qwen
resolved_category: code
resolved_owner_detail: qwen3.5:9b
human_required: false
placeholder_allowed: false
beads:
  id: toldot-t081-q06
  external_ref: T081-Q06
  gate: G3
  labels: []
