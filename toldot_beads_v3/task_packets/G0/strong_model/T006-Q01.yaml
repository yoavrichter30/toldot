id: T006-Q01
parent_task_id: T006
milestone: M00
title: Write test strategy
executor_profile: human_or_strong
depends_on:
- T002-Q01
- T003-Q01
- T005-Q01
risk: low
context_policy:
  max_input_tokens: 12000
  max_files_in_context: 8
  include_git_diff: true
  never_include_entire_repository: true
change_budget:
  allowed_write_files:
  - docs/test_strategy.md
  max_changed_lines: 250
  max_new_dependencies: 0
  max_production_files: 1
read_first:
- AGENTS.md
- docs/architecture.md
- docs/simulation_rules.md
- docs/content_format.md
objective: Define the headless testing layers and exact pass/fail expectations.
exact_instructions:
- Define parse smoke tests, unit tests, content validators, deterministic replay, save/load roundtrip, and soak
  tests.
- Define a zero exit code on success and nonzero on any failure.
- Define test naming and fixture naming.
- Define how test output includes task IDs.
- Define that no network access is permitted.
required_api_or_shapes: []
tests_or_checks_to_add:
- Do not add tests unless an allowed test file is listed.
- Do not weaken, delete, skip, or rewrite an existing test to make the task pass.
acceptance_criteria:
- The strategy maps every first-iteration risk to a test layer.
- The strategy defines the full-suite command.
- The strategy defines a maximum default test runtime target of 60 seconds for agent feedback.
validation:
  level: full
  commands:
  - test -f docs/test_strategy.md
  must_capture_exit_code: true
  must_include_relevant_failure_lines: true
forbidden:
- Do not modify files outside allowed_write_files.
- Do not refactor unrelated code.
- Do not add packages, addons, autoloads, editor plugins, or generated binaries.
- Do not invent a new API when required_api_or_shapes specifies one.
- Do not leave TODO, pass, placeholder returns, or commented-out failed attempts unless explicitly requested.
- Do not implement the runner.
stop_and_report_if:
- A required read file is missing.
- The requested API conflicts with already accepted code.
- A validation failure is outside the allowed write files.
- Completion would exceed max_changed_lines by more than 20 percent.
- Two focused repair attempts fail.
required_response:
  format: yaml
  fields:
  - task_id
  - status
  - summary
  - files_changed
  - commands_run
  - command_results
  - acceptance_checklist
  - remaining_problem
  status_values:
  - completed
  - blocked
  - failed
  no_markdown_fences: true
status: pending
resolved_owner: strong_model
resolved_category: architecture_spec
resolved_owner_detail: strong-model; human approval
human_required: false
placeholder_allowed: false
human_review_required: true
beads:
  id: toldot-t006-q01
  external_ref: T006-Q01
  gate: G0
  labels: []
